/**
 * Active synchronizations for the application
 * 
 * Add your syncs here if you need automatic behaviors.
 * Currently no syncs are needed - all routes are public.
 */

import type { Sync } from "@engine";

const allSyncs: Record<string, Sync> = {};

export default allSyncs;

